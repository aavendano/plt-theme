{%- if predictive_search.performed -%}
  <div class="l-predictive-body">
    {%- if predictive_search.resources.queries.size > 0 -%}
      <div class="l-predictive-group">
        <p class="b-is-size-7 b-has-text-grey l-predictive-label">
          {{- 'search.predictive.queries' | t -}}
        </p>
        <ul role="group">
          {%- for query in predictive_search.resources.queries -%}
            <li role="option">
              <a class="b-navbar-item" href="{{ query.url }}">
                {{- query.text -}}
              </a>
            </li>
          {%- endfor -%}
        </ul>
      </div>
    {%- endif -%}

    {%- if predictive_search.resources.products.size > 0 -%}
      <div class="l-predictive-group">
        <p class="b-is-size-7 b-has-text-grey l-predictive-label">
          {{- 'search.predictive.products' | t -}}
        </p>
        <ul role="group">
          {%- for product in predictive_search.resources.products -%}
            <li role="option">
              <a class="b-navbar-item" href="{{ product.url }}">
                {%- if product.featured_image -%}
                  <img
                    class="l-predictive-img"
                    src="{{ product.featured_image | image_url: width: 80, height: 80, crop: 'center' }}"
                    alt="{{ product.featured_image.alt | escape }}"
                    width="40"
                    height="40"
                    loading="lazy"
                  >
                {%- endif -%}
                <span>
                  <span class="b-is-block b-title b-is-size-6">{{ product.title }}</span>
                  {%- if product.price -%}
                    <span class="b-is-block b-subtitle b-is-size-7">{{ product.price | money }}</span>
                  {%- endif -%}
                </span>
              </a>
            </li>
          {%- endfor -%}
        </ul>
      </div>
    {%- endif -%}

    <div class="l-predictive-footer">
      <a
        class="b-is-size-7"
        href="{{ routes.search_url }}?q={{ predictive_search.terms | url_encode }}&options[prefix]=last"
      >
        {{- 'search.predictive.view_all' | t: terms: predictive_search.terms -}}
      </a>
    </div>
  </div>
{%- endif -%}

{% stylesheet %}
  .l-predictive-body {
    padding-block: 0.25rem;
  }

  .l-predictive-group + .l-predictive-group {
    border-top: 1px solid var(--b-border, rgb(226 232 240));
  }

  .l-predictive-label {
    padding: 0.5rem 0.75rem 0.15rem;
    text-transform: uppercase;
    letter-spacing: 0.06em;
    font-weight: 700;
    margin: 0;
  }

  .l-predictive-img {
    flex-shrink: 0;
    width: 2.5rem;
    height: 2.5rem;
    border-radius: 0.375rem;
    object-fit: cover;
    margin-right: 0.5rem;
  }

  .l-predictive-footer {
    padding: 0.6rem 0.75rem;
    border-top: 1px solid var(--b-border, rgb(226 232 240));
  }
{% endstylesheet %}

{% schema %}
{
  "name": "t:general.search",
  "tag": "section",
  "settings": []
}
{% endschema %}
