Full-width banner for promos, free-shipping notices and alerts (`b-notification`). Add `onDelete` for a dismissible × .

```jsx
<Notification color="link" light>Free discreet shipping on orders over $59.</Notification>
<Notification color="warning" onDelete={dismiss}>Only 3 left in stock.</Notification>
```
