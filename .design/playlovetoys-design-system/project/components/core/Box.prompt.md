Default surface for grouping content — white, soft shadow, 6px radius (`b-box`). Compose titles, text and actions inside.

```jsx
<Box>
  <p className="b-title b-is-4">Discreet shipping</p>
  <p>Plain packaging, no branding on the box.</p>
  <Button color="link">Learn more</Button>
</Box>
```
