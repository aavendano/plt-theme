Checkbox with inline label (`b-checkbox`). Used in filters, consent, and age-gate confirmations.

```jsx
<Checkbox defaultChecked>I am 18 or older</Checkbox>
<Checkbox>Add gift wrap (+$4)</Checkbox>
```
