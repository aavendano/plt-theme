Single-line text field (`b-input`). Pair with `Field` for label + help text. Use `state="danger"` for errors.

```jsx
<TextInput placeholder="you@example.com" type="email" />
<TextInput state="danger" defaultValue="bad@" />
```
