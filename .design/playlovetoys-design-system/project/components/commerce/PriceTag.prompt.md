Price display. On sale, pass `compareAt` — the current price turns pink and the old price is struck through.

```jsx
<PriceTag price="$49.00" />
<PriceTag price="$34.30" compareAt="$49.00" saveLabel="Save 30%" />
```
